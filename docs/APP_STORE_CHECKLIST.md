# 上架清单

> 给 Owner。第一次发 iOS App 会遇到的全部硬性要求，按「什么时候需要」排序。
> 打勾的是**已经做好的**，空的是**需要你的**。
>
> 这里只列会卡住提交的项。锦上添花的（本地化、iPad 适配、Widget）不在此列。

---

## 现在就能做（不需要 Mac，不需要产品名）

- [x] **用户协议（EULA）** —— 用 Apple 标准版，已接进代码，无需自己写
- [ ] **隐私政策网页** —— 已写好（`site/privacy.html`），需要你发布并给我网址
- [ ] **客服支持网页** —— 已写好（`site/support.html`），同上
- [ ] **落地页 / Marketing URL** —— 已写好（`site/index.html`）。选填，但审核员会点，建议一起发
- [ ] **客服邮箱** —— 一个能收信的地址就行，先用现有邮箱也可以

发布方法见 `site/README.md`，GitHub Pages 免费，十分钟能搞定。

---

## 产品名与图标

- [x] **产品名 = face3**（Owner 定于 2026-09-07）
- [x] **App 图标** 1024×1024、无透明通道 —— 已生成占位
      （`python tools/make_app_icon.py`）。**这是占位不是品牌设计**，
      拿到正式设计覆盖 `AppIcon.appiconset/icon_1024.png` 即可
- [ ] **域名** —— `face3.com` 与 `face3.app` 均已被注册（停放状态，
      不是在运营的竞品，所以无商标冲突，但拿不到）。
      可注册：`face3.io` / `face3.co` / `face3.me` / `face3app.com` / `tryface3.com`。
      **不买也能上架** —— GitHub Pages 的网址够用；但域名会写进 App 二进制与商品页，
      改一次要发一次更新，所以要在提交前定下来。
      完整判断见 [`DOMAIN_AND_GROWTH.md`](DOMAIN_AND_GROWTH.md)

---

## 需要 Apple 开发者账号

- [ ] **Apple Developer Program** 99 美元/年
      免费账号只能装到自己手机上、7 天过期；**上架必须付费账号**
- [ ] **App Store Connect 里建 App 记录**
- [ ] **配置订阅商品** —— 月费 $4.99、年费 $29.99（你已定）
      商品 ID 要和代码里一致：
      `com.faceritual.premium.monthly` / `com.faceritual.premium.yearly`

---

## 隐私标签（提交时苹果会逐项问你）

好消息：按现在的代码，全部可以选 **"Data Not Collected"（不收集数据）**。

| 苹果会问 | 你的答案 | 依据 |
| --- | --- | --- |
| 收集联系信息吗 | 否 | 没有账号系统 |
| 收集健康数据吗 | 否 | 练习记录只存本地，不上传 |
| 收集照片/视频吗 | 否 | 摄像头帧只在内存过一遍就丢 |
| 收集位置吗 | 否 | 没用定位 |
| 用于追踪吗 | 否 | 没有广告标识符、没有第三方 SDK |

⚠️ **如果以后接入第三方 analytics**（规格 §19 待决），这张表、隐私政策、
App 内的摄像头说明**必须一起改** —— 隐私标签填错是常见的下架原因。

---

## 提交材料

- [ ] **截图** —— 至少 6.7 吋（iPhone 15/16 Pro Max 尺寸）一组
      ⚠️ 必须用**正式内容**截，现在满屏 MOCK 角标，不能拿去提交
- [ ] **App 描述 / 关键词 / 推广文本**
      ⚠️ 措辞要守规格 §4/§13：不得出现抗衰、除皱、穴位疗效、「年轻 X 岁」这类表述。
      健康美容类的功效宣称是苹果审核的重点
- [ ] **分级问卷** —— 大概率是 4+
- [ ] **审核备注** —— 建议主动写清：
      「App 的核心是跟着示范视频做三分钟面部按摩。下半屏的摄像头镜像是**可选**的，
        仅作镜子用，不做任何人脸识别或分析；不开摄像头也能完整使用。
        摄像头画面全程在设备端处理，不录制、不上传。」
      摄像头权限是审核关注点，主动说清用途能少一轮打回

---

## 会被拒的常见原因（对照自查）

| 原因 | 我们的状态 |
| --- | --- |
| Paywall 缺 Terms / Privacy 链接 | ⚠️ 缺隐私政策 URL |
| 摄像头用途说明太含糊 | ✅ 已写清楚 |
| 隐私标签与实际行为不符 | ✅ 目前一致，加 SDK 后要复查 |
| 订阅信息不全（价格/周期/自动续订） | ✅ 已按 3.1.2 补齐 |
| 健康类功效宣称 | ⚠️ 取决于你写的 App 描述 |
| 内容明显未完成 | ⚠️ 内容全部标 `draft`，**未过专家审核不应提交** |
| 有摄像头权限但看不出为什么需要 | ✅ 镜像是可见功能，且明确可选 |

---

## 顺序建议

产品形态已于 2026-09-07 定为「上面示范视频，下面自己的镜像」，
AR Mirror 与 Watch & Breathe 已移除，所以原来第 3 步的 AR Go/No-Go 判定不再存在。

1. [x] **发布隐私政策与支持页** —— 已上线（GitHub Pages）
2. [ ] **填两个占位符**：发布者名义、客服邮箱。
       ⚠️ 客服邮箱会**公开显示在网页上**，建议别用主邮箱
3. [ ] **付费开发者账号 + App Store Connect 配置订阅商品**
       ⚠️ 这一步没做完，订阅在 Release 里拿不到商品，Paywall 是空的
4. [ ] **真机跑一次**：装到 iPhone 上，走一遍完整 routine + 沙盒购买
       ⚠️ App **从未在真机上开过机**，CI 用的是模拟器
5. [ ] **专家审 20 个动作**（`make review` 生成审阅表）
6. [ ] **示范视频**（`docs/COACH_VIDEO_SPEC.md`）——
       ⚠️ 这一项**不挡上架**：缺素材会回落到示意动画，功能完整
7. [ ] 截图 → 提交

第 2 步十分钟就能做完，做完上架的两个必填网址就彻底齐了。
第 3、4 步是现在真正的瓶颈，两件都只有你能做。
